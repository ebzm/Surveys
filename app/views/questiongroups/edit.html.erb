<div class="container">
  <%=  form_with model: @questiongroup do |form| %>
    <p><%= form.text_area :label, class: 'form-text-area' %></p>
    <p class="mt"><%= form.submit 'Сохранить', class: 'button button-main' %></p>
  <% end %>

  <p class="mt"><%= link_to "назад", "/surveys/#{@questiongroup[:survey_id]}" %></p>
</div>