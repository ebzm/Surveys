<div class="container">
  <%=  form_with model: [@survey, @question_group] do |form| %>
    <p><%= form.text_area :label, class: 'form-text-area' %></p>
    <p class="mt"><%= form.submit 'Create', class: 'button button-main' %></p>
  <% end %>

  <p class="mt"><%= link_to "back", survey_path(@survey) %></p>
</div>