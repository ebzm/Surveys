<div class="container">
  <%=  form_with model: [@survey, @questiongroup] do |form| %>
    <p><%= form.text_area :label, class: 'form-text-area' %></p>
    <p class="mt"><%= form.submit 'Save', class: 'button button-main' %></p>
  <% end %>

  <p class="mt"><%= link_to "back", survey_path(@survey) %></p>
</div>