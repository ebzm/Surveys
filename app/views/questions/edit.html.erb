<div class="container">
  <%=  form_with model: @question do |form| %>
    <p><%= form.text_area :questiontype, class: 'form-text-area' %></p>
    <p class="mt"><%= form.submit 'Сохранить', class: 'button button-main' %></p>
  <% end %>

  <p class="mt"><%= link_to "назад", "/questiongroups/#{@question[:questiongroup_id]}" %></p>
</div>